interface:
  display_name: "Zirium Custom Format"
  short_description: "Build registry formats for installed Zirium"
  default_prompt: "Use $zirium-custom-format to research this repository’s MLIR dialects and create a registry for my installed Zirium binary."
policy:
  allow_implicit_invocation: false
