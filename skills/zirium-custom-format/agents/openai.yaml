interface:
  display_name: "Zirium Custom Format"
  short_description: "Build custom registry formats for Zirium"
  default_prompt: "Use $zirium-custom-format to research this repository’s MLIR dialects and create a Zirium registry using documentation matching the project's Zirium version."
policy:
  allow_implicit_invocation: false
