interface:
  display_name: "Zirium Custom Format"
  short_description: "Build custom registry formats for Zirium"
  default_prompt: "Use $zirium-custom-format to research this repository’s MLIR dialects and create a Zirium registry using the latest GitHub documentation."
policy:
  allow_implicit_invocation: false
